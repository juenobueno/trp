package therobotpeople.urcap;

public class PointsToURScript {
	//This class will take in a list of points and convert them into the necessary URSCRIPT
	
	public void convert(String points) {
		//The points should be one massive string, that is split on new lines
		//The points will need to be structured to contain
		// [x,y,z, orientation]
		
		
		//Move to pick up point
		//Pick up box
		//Move to entry point which is 1 box above the position
		//Move box into position and drop it off
		//Move to entry point
		//Move to pick up point
		
		
	}
}
